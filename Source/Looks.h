#pragma once

#include <JuceHeader.h>

static const juce::Colour lightBlue(0u, 125u, 125u);
static const juce::Colour midBlue(0u, 70u, 70u);
static const juce::Colour darkBlue(0u, 19u, 17u);
static const juce::Colour dimGrey(66u, 66u, 66u);
